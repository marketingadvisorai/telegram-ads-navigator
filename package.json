{
  "name": "telegram-ads-navigator",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Minimal Telegram Ads Navigator MVP",
  "scripts": {
    "start": "node src/index.js",
    "simulate": "node scripts/simulate-flow.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
